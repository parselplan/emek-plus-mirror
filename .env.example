# Harici auth API (opsiyonel). Tanımlı değilse geliştirme modunda sunucu tarafı OTP kullanılır.
# AUTH_API_BASE_URL=https://api.example.com

# Development (NODE_ENV !== production): sabit OTP 123456 geçerlidir.
# Production'da rastgele OTP üretilir; 123456 kabul edilmez.

# Şifreli HttpOnly oturum çerezi için gizli anahtar (Lovable/production'da zorunlu)
# SESSION_SECRET=your-long-random-secret-at-least-32-chars

# Lovable preview: OTP log + sabit 123456 (worker'lar arası OTP store sorununu önler)
# EMEK_LOG_OTP=true
# EMEK_DEV_OTP_SECRET=emek-dev-otp-local

# npm run dev aynı terminalde Lovable OTP göstermek için (.env.local önerilir):
# OTP_WATCH_URL=https://id-preview--<project-id>.lovable.app
# EMEK_DEV_OTP_PHONE=5535903359

# Beklenen harici endpoint'ler:
# POST /auth/otp/send   { "phone": "5xxxxxxxxx" }
# POST /auth/otp/verify { "phone": "5xxxxxxxxx", "code": "123456" }
# GET  /auth/me         Authorization: Bearer <token>
# verify yanıtı: { "token": "...", "user": { "id": "...", "phone": "5xxxxxxxxx" }, "expiresAt": 1234567890 }
